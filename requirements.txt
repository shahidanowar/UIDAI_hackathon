# Core Flask
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.0
SQLAlchemy==2.0.23
Werkzeug==3.0.1

# For Vercel
python-dotenv==1.0.0

# Note: pandas, numpy, scikit-learn removed for Vercel deployment
# The app uses rule-based prediction fallback and mock data
# which don't require these heavy packages
